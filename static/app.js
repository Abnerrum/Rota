let maps=[];const center=[-16.71,-49.27];async function load(){let d=await (await fetch('/api/dashboard')).json();ordersN.textContent=d.orders;pendingN.textContent=d.pending;techN.textContent=d.technicians;let os=await(await fetch('/api/orders')).json(),ts=await(await fetch('/api/technicians')).json();orderList.innerHTML=os.map(o=>'<div class="row"><span><b>#'+o.id+' '+o.customer+'</b><br><small>'+o.address+'</small></span><span class="badge">'+o.priority+'</span></div>').join('');techList.innerHTML=ts.map(t=>'<div class="row"><b>'+t.name+'</b><span class="badge">'+t.status+'</span></div>').join('');draw('miniMap',os,ts)}function draw(id,os=[],ts=[]){let el=document.getElementById(id);if(el._leaflet_id)return;let m=L.map(id).setView(center,11);L.tileLayer('https://{s}.tile.openstreetmap.org/{z}/{x}/{y}.png',{attribution:'© OpenStreetMap'}).addTo(m);os.forEach(o=>L.marker([o.lat,o.lng]).addTo(m).bindPopup('OS #'+o.id+' — '+o.customer));ts.forEach(t=>L.circleMarker([t.lat,t.lng],{radius:9}).addTo(m).bindPopup('Técnico: '+t.name));}async function optimize(){show('map');let data=await(await fetch('/api/optimize',{method:'POST'})).json();routesN.textContent=data.routes.length;let os=data.routes.map(x=>x.order),ts=data.routes.map(x=>x.technician);setTimeout(()=>draw('bigMap',os,ts),50);result.innerHTML='<h3>Distribuição sugerida</h3>'+data.routes.map(x=>'<div class="row"><span>OS #'+x.order.id+' → <b>'+x.technician.name+'</b></span><span>'+x.distance_km+' km</span></div>').join('')}function show(id){['dashboard','map','orders','techs'].forEach(x=>document.getElementById(x).classList.toggle('hidden',x!==id));title.textContent={dashboard:'Visão Operacional',map:'Planejamento de Rotas',orders:'Ordens de Serviço',techs:'Equipes'}[id];if(id==='map')setTimeout(async()=>{let os=await(await fetch('/api/orders')).json(),ts=await(await fetch('/api/technicians')).json();draw('bigMap',os,ts)},50)}load();